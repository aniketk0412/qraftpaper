DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
AUTH_SECRET="replace-with-a-random-32-plus-character-secret"
# Public site origin — used for canonical URLs, sitemap, robots and OG images.
NEXT_PUBLIC_SITE_URL="https://qraftpaper.vercel.app"
# Email shown across the footer, legal pages and "Forgot password?" mailto.
# Set this to a real inbox you actually read.
NEXT_PUBLIC_SUPPORT_EMAIL="support@qraftpaper.app"
OPENROUTER_API_KEY="sk-or-v1-..."
# AI models — any tool-calling model on OpenRouter. Defaults to Claude Haiku (cheap).
# For higher question-craft quality at similar cost, try google/gemini-2.5-flash;
# for top quality (pricier) anthropic/claude-sonnet-4.6. Confirm slugs at
# https://openrouter.ai/models
OPENROUTER_GENERATION_MODEL="anthropic/claude-haiku-4.5"
OPENROUTER_EXTRACTION_MODEL="anthropic/claude-haiku-4.5"
LEMONSQUEEZY_API_KEY=""
LEMONSQUEEZY_STORE_ID=""
LEMONSQUEEZY_WEBHOOK_SECRET=""
LEMONSQUEEZY_VARIANT_EDUCATOR=""
# One-time $1 "3-Day Pass" trial. Create a single-payment (non-subscription)
# product in Lemon Squeezy and paste its variant id here. Until it's set, the
# trial CTA on /billing stays "Available soon" and trial checkout is refused.
LEMONSQUEEZY_VARIANT_TRIAL=""
# Transactional email — used for password-reset links. Without RESEND_API_KEY
# the send is skipped and reset emails never go out, so set this before you
# expose /forgot-password. EMAIL_FROM must be a verified sender for production;
# onboarding@resend.dev only works in Resend's sandbox.
RESEND_API_KEY=""
EMAIL_FROM="QraftPaper <onboarding@resend.dev>"
# Cloudflare Turnstile (signup bot check). Leave blank to disable the captcha.
# Get keys at https://dash.cloudflare.com → Turnstile
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""
# PostHog product analytics (signup → paper_generated → subscription_activated).
# Leave NEXT_PUBLIC_POSTHOG_KEY blank to disable. Free tier covers 1M events / month.
# Get a project key at https://posthog.com
NEXT_PUBLIC_POSTHOG_KEY=""
NEXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"
# Google AdSense. Set to your publisher id (looks like ca-pub-1234567890123456)
# to switch on ads end-to-end: the loader script, a correct /ads.txt, and the
# CSP allowances all key off this one variable. Leave blank to ship with no ad
# code at all.
NEXT_PUBLIC_ADSENSE_CLIENT=""
